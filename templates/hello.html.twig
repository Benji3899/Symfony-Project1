{% extends 'base.html.twig' %}

        {% block stylesheets %}
            {# encore_entry_link_tags('app') #}
            {{ parent() }}
        {% endblock %}

{% block body %}
    <h1>HomePage</h1>
    <p>Hello {{ name }}</p>
{% endblock %}